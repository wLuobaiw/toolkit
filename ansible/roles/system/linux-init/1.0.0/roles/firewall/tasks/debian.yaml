---
- name: Install ufw
  apt:
    name: ufw
    state: present

- name: Allow SSH through ufw
  ufw:
    rule: allow
    port: "{{ ssh_port }}"
    proto: tcp

- name: Enable ufw with default deny incoming
  ufw:
    state: enabled
    default: deny
    direction: incoming
